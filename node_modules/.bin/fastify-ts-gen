#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../fastify-typescript-generator/bin/fastify-typescript-generator.js" "$@"
else 
  exec node  "$basedir/../fastify-typescript-generator/bin/fastify-typescript-generator.js" "$@"
fi
